import leftHandRaw from "./left-hand.svg?raw";
import rightHandRaw from "./right-hand.svg?raw";

/**
 * Shared, standalone SVG assets for the two typing hands, rendered by the
 * on-keyboard `HandOverlay`. The artwork is extracted verbatim from
 * `hands.svg` (neutral resting posture) so the visual identity of the
 * typing-club hands is preserved exactly.
 */
export const leftHandSvg = leftHandRaw;
export const rightHandSvg = rightHandRaw;
