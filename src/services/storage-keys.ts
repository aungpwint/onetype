const PREFIX = 'onetype:'

export const UI_KEYS = {
    theme: `${PREFIX}theme`,
    themePreset: `${PREFIX}theme-preset`,
    sound: `${PREFIX}sound`,
    focusMode: `${PREFIX}focus-mode`,
    lessonMode: `${PREFIX}lesson-mode`,
} as const
